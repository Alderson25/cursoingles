
export interface Lesson {
  id: number;
  title: string;
  description: string;
  icon: string;
}

export interface Testimonial {
  name: string;
  role: string;
  text: string;
  location: string;
}

export interface LeadData {
  name: string;
  email: string;
  whatsapp: string;
}
